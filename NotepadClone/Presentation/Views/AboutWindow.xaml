<Window x:Class="NotepadClone.Presentation.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About"
        Height="220"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Notepad Clone"
                   FontWeight="Bold"
                   FontSize="18"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,6">
            <TextBlock Text="Student: "
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="StudentNameText"/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,6">
            <TextBlock Text="Grupa: "
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="GroupNameText"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Margin="0,0,0,6">
            <TextBlock Text="Email: "
                       FontWeight="SemiBold"/>
            <TextBlock>
                <Hyperlink x:Name="EmailHyperlink"
                           RequestNavigate="EmailHyperlink_RequestNavigate"/>
            </TextBlock>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="Tema 1 pentru Medii Avansate de Programare"
                   Foreground="Gray"
                   VerticalAlignment="Bottom"/>

        <Button Grid.Row="5"
                Content="Close"
                Width="90"
                HorizontalAlignment="Right"
                IsDefault="True"
                Margin="0,12,0,0"
                Click="CloseButton_Click"/>
    </Grid>
</Window>
